## 入力

{{ .InputMarkdown }}

{{- if .CurrentResultMarkdown }}
## 現在の結果

{{ .CurrentResultMarkdown }}

## 再生成指示

- 現在の結果を、以下のコメントをもとに修正してください。
- 変更後は、複数の改善提案から同じファイルを上書きする前提で、改善後の内容を記載してください。

## コメント

{{ .Source.Comment }}
{{- end }}

## 既存改善項目

{{- if .ExistingImprovements }}
{{- range .ExistingImprovements }}
- {{ .Title }} ({{ .Path }}) / phases: {{ range $index, $phase := .Phase }}{{ if $index }}, {{ end }}{{ $phase }}{{ end }}
{{- end }}
{{- else }}
- なし
{{- end }}

## 対象コンテキスト

- repository: {{ .Repository }}
- issueNumber: {{ .IssueNumber }}
- sourceEvent: {{ .Source.EventType }}
- phases: {{ range $index, $phase := .Phases }}{{ if $index }}, {{ end }}{{ $phase }}{{ end }}
{{- if not .CurrentResultMarkdown }}

## 再生成指示

- 現在の結果は渡されていません。
- コメントがある場合は、それをもとに改善案を再生成してください。
{{- end }}
