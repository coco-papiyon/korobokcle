channels:
  - name: windows-toast
    type: windows_toast
    events:
      - design_ready
      - waiting_design_approval
      - implementation_ready
      - review_ready
      - failed
    enabled: true
