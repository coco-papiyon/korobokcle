channels:
    - name: windows-toast
      type: windows_toast
      events:
        - waiting_design_approval
        - waiting_final_approval
        - review_completed
        - pr_created
        - failed
      enabled: true
